// set camera
cam = view_camera[0] // set viewport
follow = o_player // set object to follow
view_w = camera_get_view_width(cam) * 0.5 // get width and half it
view_h = camera_get_view_height(cam) * 0.5 // get height and half it
xTo = xstart // x original position
yTo = ystart // y original position